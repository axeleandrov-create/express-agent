services:
  - type: web
    name: na-chto-stavit
    runtime: node
    region: frankfurt
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "20"
